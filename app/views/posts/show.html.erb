<h1>CHAT</h1>
<h2>投稿詳細</h2>
<div class="post">
  <p><%= @post.name %></p>
  <p><%= @post.about %></p>
  <p><%= @post.created_at %></p>
</div>
<%= link_to "編集する", edit_post_path(@post.id) %>
<%= link_to "投稿一覧に戻る", posts_path %>